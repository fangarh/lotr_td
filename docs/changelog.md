# Changelog

## 2026-05-03

- Implemented the scenario selection/loading foundation for the two-map desktop MVP corridor: added `scenario_index.json`, two MVP scenario files, `SpikeScenarioCatalog`, runtime scenario switching in `Main`, a compact HUD scenario selector, and smoke coverage for catalog loading, scenario switching, main scene data shape, and HUD selection wiring. Build spots, tower purchasing, economy, upgrades, balance, and campaign persistence remain out of scope for this block.
- Added the `Godot Two-Map MVP Corridor Design` spec at `docs/superpowers/specs/2026-05-03-godot-two-map-mvp-corridor-design.md`, defining the desktop-first MVP target as two authored scenario-driven maps with build spots, four tower roles, four enemy roles, economy, one upgrade level per tower, HUD-driven win/loss/restart flow, and gameplay-first implementation order.
- Updated Obsidian notes to shift the next Godot direction from more isolated spike slices toward the two-map playable MVP corridor while keeping Android/touch-first UX, free placement, upgrade branches, final animations/VFX, path rerouting, full campaign progression, DLC/monetization, and complex enemy abilities out of MVP scope.
- Added the `Godot Blocker Contact Readability Capture Design` spec and implementation plan at `docs/superpowers/specs/2026-05-03-godot-blocker-contact-readability-capture-design.md` and `docs/superpowers/plans/2026-05-03-godot-blocker-contact-readability-capture.md`.
- Added `tests/capture_blocker_contact_preview.gd`, a capture-only Godot viewport script that waits for the existing `orc-blockade` runtime blocker, verifies an already-spawned enemy reports `current_blocker_id()` at contact, verifies the existing enemy-to-adapter damage bridge affects blocker HP, and saves `builds/previews/blocker_contact_preview.png` with capture-only `BlockerContactReviewMarker` helpers.
- Extended `smoke_obstacle_proxy.gd` to lock the blocker-contact capture contract: capture script path, output PNG name, desktop `1280x720` viewport, adapter `get_blockers()` usage, enemy `current_blocker_id()` verification, and capture-only marker naming.
- Updated Obsidian notes for blocker-contact readability while keeping production blocker visuals, attack animations, final VFX, path rerouting, stacking policy, queueing, rewards, score, gold, wave/game-state ownership, tower retargeting, balance, upgrades, costs, and production placement UI out of scope.
- Added the `Godot Enemy Blocker Contact Design` spec and implementation plan at `docs/superpowers/specs/2026-05-03-godot-enemy-blocker-contact-design.md` and `docs/superpowers/plans/2026-05-03-godot-enemy-blocker-contact.md`.
- Implemented the first enemy/blocker contact interaction: `PlaceholderEnemy` now accepts blocker snapshots, reports `current_blocker_id()`, stops while touching a blocker radius, and emits `blocker_attack_requested(blocker_id, amount)` on a small spike cadence.
- Wired `Main` to synchronize `SpikeObstacleTowerAdapter.get_blockers()` output into spawned enemies and bridge enemy blocker attack signals back into `SpikeObstacleTowerAdapter.apply_blocker_damage()`.
- Extended `smoke_enemy_proxy.gd` and `smoke_obstacle_proxy.gd` with TDD coverage for blocker contact stop, attack signal output, movement resume after blocker removal, and main-scene adapter damage wiring.
- Updated Obsidian notes for enemy/blocker contact while keeping path rerouting, stacking policy, attack animations, final VFX, spawned blocker visuals, rewards, score, gold, wave/game-state ownership, tower retargeting, balance, upgrades, costs, and production placement UI out of scope.
- Added the `Godot Temporary Blocker HP Lifecycle Implementation Plan` at `docs/superpowers/plans/2026-05-03-godot-temporary-blocker-hp-lifecycle.md`.
- Implemented the first temporary blocker HP/lifetime lifecycle in `SpikeObstacleTowerAdapter`: runtime blockers now spawn from `effect: "temporary-blocker"` tower obstacle effects, expose read-only blocker dictionaries, support `get_blocker_count()`, `get_blockers()`, and `apply_blocker_damage(blocker_id, amount)`, and are removed by lethal damage or optional lifetime expiry.
- Added the `orc-blockade` temporary blocker fixture to `spike_scenario.json` and wired `shadow-tower-b3` to spawn it for main-scene smoke coverage while leaving the existing B2 timed slow-zone contract intact.
- Extended `smoke_obstacle_proxy.gd` with TDD coverage for blocker API presence, spawn position/source id/radius/HP/lifetime output, read-only copies, non-negative damage clamping, non-lethal damage, lethal removal, lifetime expiry, reset behavior, and main-scene registration.
- Updated Obsidian notes for the blocker HP lifecycle implementation while keeping enemy movement blocking, enemy blocker attacks, path rerouting, stacking policy, tower retargeting, rewards, wave/game-state ownership, production placement UI, balance, and final spawned blocker visuals out of scope.
- Added the `Godot Temporary Blocker HP Contract Design` spec at `docs/superpowers/specs/2026-05-03-godot-temporary-blocker-hp-contract-design.md`, accepting adapter-owned runtime blocker HP/lifetime lifecycle as the next obstacle boundary while keeping enemy movement blocking, enemy blocker attacks, path rerouting, stacking policy, tower retargeting, rewards, production placement UI, balance, and final spawned blocker visuals out of scope.
- Recorded the temporary blocker HP decision in `docs/decisions.md` and updated `docs/roadmap.md`, `docs/mechanics.md`, `docs/visual-style.md`, `docs/lore-and-factions.md`, and `docs/android-porting.md`; at that point the next implementation step was a TDD plan for blocker lifecycle only.
- Added `tests/capture_timed_slow_zone_preview.gd`, a local Godot viewport capture that waits for a tower-owned timed slow zone, verifies an already-spawned enemy has an active slow multiplier inside it, and saves `builds/previews/timed_slow_zone_preview.png`.
- Extended `smoke_obstacle_proxy.gd` to lock the timed slow-zone capture contract: capture script path, output PNG name, desktop `1280x720` viewport, timed slow-zone count wait, active multiplier assertion, and capture-only `TimedSlowZoneReviewMarker`.
- Added the `Godot Timed Slow-Zone Readability Capture Design` spec at `docs/superpowers/specs/2026-05-03-godot-timed-slow-zone-readability-capture-design.md` and the implementation plan at `docs/superpowers/plans/2026-05-03-godot-timed-slow-zone-readability-capture.md`, keeping blocker HP, path rerouting, stacking policy, enemy blocker attacks, production placement UI, rewards, damage, balance, and production spawned obstacle props out of scope.
- Implemented live slow-zone propagation in `Main`: after `SpikeObstacleTowerAdapter.advance(delta)`, current adapter slow zones are pushed into spawned enemy proxies, and newly spawned enemies use the same helper for initial slow-zone setup.
- Extended `smoke_obstacle_proxy.gd` so the main-scene contract now verifies an already-spawned enemy receives the timed tower-owned slow zone after `Main._process()` advances the obstacle adapter.
- Updated Obsidian notes for live slow-zone propagation while keeping blocker HP, path rerouting, stacking policy, enemy blocker attacks, visual spawned props, production placement UI, rewards, damage, and balance out of scope.
- Added the `Godot Live Slow-Zone Propagation Design` spec at `docs/superpowers/specs/2026-05-03-godot-live-slow-zone-propagation-design.md`, choosing a narrow runtime synchronization step so timed tower-owned slow zones affect already-spawned enemies without adding blocker HP, path rerouting, stacking policy, enemy blocker attacks, visual spawned props, production placement UI, rewards, damage, or balance changes.
- Recorded the live slow-zone propagation decision in `docs/decisions.md` and updated `docs/roadmap.md` so the next implementation step is a TDD slice in `Main`/`smoke_obstacle_proxy.gd`.
- Implemented timed tower-owned slow-zone spawning in `SpikeObstacleTowerAdapter`: registered tower proxies can now resolve an obstacle effect id, tick a spawn interval, create temporary runtime slow-zone dictionaries at nearby path points, and expire those zones after their configured lifetime.
- Wired timed slow-zone spawning through `Main`: path points are passed into `ObstacleTowerAdapter`, towers are registered with obstacle catalog data, and the adapter advances every frame without changing `SpikeTowerAttackAdapter` damage ownership.
- Added timed obstacle fields to `shadow-tower-b2` in `spike_scenario.json`: `obstacleEffectId`, `obstacleSpawnInterval`, `obstacleSpawnLifetime`, and `obstacleSpawnRange`.
- Extended `smoke_obstacle_proxy.gd` to cover timed slow-zone spawn cadence, lifetime expiry, nearest-path placement, missing-effect refusal, registered tower counts, and main-scene timed spawn wiring.
- Added the `Godot Timed Slow-Zone Spawning Design` spec at `docs/superpowers/specs/2026-05-03-godot-timed-slow-zone-spawning-design.md`, choosing timed slow-zone spawning as the first tower-owned obstacle behavior while keeping blocker HP, path rerouting, stacking rules, enemy blocker attacks, visual spawned props, production placement UI, rewards, damage, and balance out of scope.
- Recorded the timed slow-zone spawning decision in `docs/decisions.md` and updated `docs/roadmap.md` so the next implementation step is a TDD slice in `SpikeObstacleTowerAdapter`.
- Added `scripts/spike_obstacle_tower_adapter.gd`, a narrow Godot scene-level adapter that registers runtime obstacle effects from existing JSON obstacle placements and exposes read-only slow-zone dictionaries.
- Added `ObstacleTowerAdapter` to `scenes/main.tscn` and moved slow-zone effect collection out of `Main`; `Main` now resets/registers through the adapter and passes adapter-provided slow zones to spawned enemies.
- Extended `smoke_obstacle_proxy.gd` with TDD coverage for the adapter script, main-scene adapter node, slow-zone registration, visual-only registration behavior, reset behavior, and main-scene adapter ownership of placement-derived slow zones.
- Added the `Godot Obstacle Tower Ownership Design` spec at `docs/superpowers/specs/2026-05-03-godot-obstacle-tower-ownership-design.md`, choosing a narrow `SpikeObstacleTowerAdapter` boundary before blocker HP, stacking rules, path rerouting, enemy blocker attacks, obstacle lifetime, tower cooldown spawning, or production placement UI.
- Recorded the obstacle ownership decision in `docs/decisions.md` and updated `docs/roadmap.md` so the next implementation step is a TDD slice that moves slow-zone effect registration out of `Main` while preserving current slow-zone behavior.
- Added `tests/capture_spike_slow_zone_preview.gd`, a local Godot viewport capture for slow-zone readability that validates an active enemy slow multiplier before saving `builds/previews/spike_slow_zone_preview.png`.
- Added capture-only `SlowZoneReviewMarker`, ring, and label helpers inside the slow-zone capture script so the current obstacle slow effect can be reviewed in PNG form without adding production UI, obstacle placement UI, path blocking, rerouting, blocker HP, combat ownership, or balance changes.
- Extended `smoke_obstacle_proxy.gd` to lock the slow-zone capture contract: capture script path, desktop `1280x720` viewport, output PNG name, multiplier assertion, and capture-only marker.
- Added the first Godot obstacle slow-zone gameplay spike: `corrupted-roots` obstacle catalog data now declares `effect: "slow-zone"`, `slowMultiplier`, and `radius`.
- Extended `Main` to collect placed obstacle slow zones in world space and pass them to spawned `PlaceholderEnemy` proxies without moving pathing, combat, wave-state, game-state, restart, reward, or damage ownership.
- Extended `PlaceholderEnemy` with `set_slow_zones()`, `get_slow_zone_count()`, and `current_slow_multiplier()`, applying the active slow multiplier during local path movement.
- Extended `smoke_enemy_proxy.gd` and `smoke_obstacle_proxy.gd` to lock enemy slow movement, multiplier reset outside zones, and main-scene slow-zone wiring from obstacle placements.
- Added the design and implementation notes for the slow-zone spike at `docs/superpowers/specs/2026-05-03-godot-obstacle-slow-zone-spike-design.md` and `docs/superpowers/plans/2026-05-03-godot-obstacle-slow-zone-spike.md`.
- Added `tests/capture_spike_hud_action_states.gd`, a dedicated Godot HUD action-state capture script that prepares `wave_clear`, `basic_win`, and `basic_loss` through existing `Main`/adapter hooks and verifies `Next wave` / `Restart` button text before saving PNGs.
- Added six terminal HUD action preview captures: `spike_hud_wave_clear_preview.png`, `spike_hud_wave_clear_mobile_landscape_preview.png`, `spike_hud_basic_win_preview.png`, `spike_hud_basic_win_mobile_landscape_preview.png`, `spike_hud_basic_loss_preview.png`, and `spike_hud_basic_loss_mobile_landscape_preview.png`.
- Extended `smoke_spike_hud.gd` to lock the new terminal HUD action capture contract, including desktop `1280x720`, rotated-phone landscape `844x390`, explicit `wave_clear` / `basic_win` / `basic_loss` states, and the expected output PNG names.
- Added the first Godot HUD terminal action shell: `SpikeHud` now creates a compact `ActionButton`, hidden during `running`, showing `Next wave` at `wave_clear`, and showing `Restart` at `basic_win` / `basic_loss`.
- Wired HUD actions through signals only: `SpikeHud` emits `next_wave_requested` and `restart_requested`, while `Main` remains responsible for calling `start_next_wave_manually()` and `restart_current_scenario_manually()`.
- Extended `smoke_spike_hud.gd` to cover the HUD action button visibility/text, next-wave action wiring, restart action wiring after `basic_loss`, and continued narrow mobile layout coverage.
- Refreshed the existing Godot HUD preview captures after adding the action shell: `spike_hud_preview.png` and `spike_hud_mobile_landscape_preview.png` were regenerated through non-headless Godot viewport capture.
- Added a debug-only Godot manual restart input hook: pressing `R` in `Main._unhandled_input` calls `restart_current_scenario_manually()`, so it only works after `basic_win` or `basic_loss` and does not add HUD controls, production restart UI, auto-retry, balance changes, or new gameplay ownership.
- Extended `smoke_game_state_adapter.gd` to cover the `R` key boundary: no restart from non-terminal `wave_clear`, restart after `basic_win`, and restart after `basic_loss` with restored first-wave runtime state.
- Added a narrow manual scenario restart boundary for the Godot spike: `Main.restart_current_scenario_manually()` now runs only after `basic_win` or `basic_loss`, rebuilds the current JSON-backed scenario runtime, resets combat kill/reward/tracked state, resets tower attack registrations/review visuals, and starts the first wave again without adding UI or automatic retry.
- Extended `smoke_game_state_adapter.gd` to cover restart refusal while `running` or `wave_clear`, restart after both `basic_win` and `basic_loss`, restored base lives/breach count, reset kill/reward totals, one first-wave tracked enemy, and non-duplicated tower attack registrations.
- Corrected the Godot mobile HUD preview contract to use a rotated-phone landscape viewport: `capture_spike_hud_mobile_preview.gd` now captures `844x390` and writes `builds/previews/spike_hud_mobile_landscape_preview.png`.
- Extended `smoke_spike_hud.gd` so the HUD smoke suite fails if the mobile capture script regresses to portrait `390x844` or an orientation-ambiguous output name.

## 2026-05-02

- Implemented the selected read-only Godot HUD option: `SpikeHud` now renders a mobile-safe top `HudBar` with state, wave, lives, breaches, and active enemies, wraps to two rows on narrow mobile widths, keeps `debug_text()` for smoke diagnostics, and still creates no next-wave button or gameplay ownership.
- Extended `smoke_spike_hud.gd` to lock the new HudBar/value-label boundary plus narrow mobile wrap behavior, and added/refreshed `capture_spike_hud_mobile_preview.gd`, `spike_hud_preview.png`, and `spike_hud_mobile_preview.png` for visual review.
- Added `docs/mockups/hud-options.html`, a browser-viewable comparison of three possible next HUD directions: read-only mobile-safe bar, debug panel with next-wave action, and fuller production HUD shell.
- Tuned the temporary B4 projectile readability preview: `ProjectileVisual` now carries an ember glow and short trail helper, the preview model is slightly larger, `smoke_tower_attack_adapter.gd` verifies the helper nodes, and `tower_attack_readability_preview.png` was refreshed.
- Added the tower attack readability capture-only pass: `capture_tower_attack_readability_preview.gd` freezes adapter auto-advance for a stable review frame, forces the existing tower attack path, verifies `TowerFireCue` and `ProjectileVisual`, and saves `builds/previews/tower_attack_readability_preview.png` without changing VFX parameters, damage timing, targeting, balance, or projectile behavior.
- Recorded the next Godot tower attack readability work as two Obsidian-tracked passes: first capture-only, then optional readability tuning only if the capture shows a problem.
- Added `smoke_scenario_progression.gd` to lock the two-wave endpoint-breach progression fixture: first breach consumes one configured life and leaves `wave_clear`, manual next-wave starts the second wave, second breach reaches `basic_loss`, HUD readout refreshes, and endpoint breaches still grant no kills or rewards.
- Added a Godot HUD readability capture pass: `SpikeHud` now creates a translucent `DebugPanel` behind the readout, `smoke_spike_hud.gd` verifies the panel/label boundary, and `capture_spike_hud_preview.gd` saves `builds/previews/spike_hud_preview.png` for local visual review.
- Added the first Godot debug HUD boundary: `spike_hud.gd` renders a compact read-only Control readout for state, wave id/index, lives, breach count, and active enemies; `Main` binds it to the existing game/wave/combat adapters and refreshes it without adding buttons or gameplay ownership.
- Added scenario-configured base lives to the Godot spike: `spike_scenario.json` and the two-wave smoke fixture now include `gameState.baseLives`, `Main` applies it to `SpikeGameStateAdapter.configure_base_lives()` before first-wave start, and smoke coverage verifies non-lethal endpoint breaches decrement configured lives without kill/reward accounting.
- Adopted a shorter context-reset handoff policy: future resume prompts should rely on Obsidian docs and code for detailed state, carrying only required read rules, verification rules, the last completed step, and the next recommended step.
- Added endpoint breach behavior to `PlaceholderEnemy`: reaching the final path point emits `path_breached(enemy)` once, hides/stops the proxy, and removes it from the live targeting pool instead of looping back to the start.
- Connected `Main` to enemy endpoint breaches: it calls `SpikeGameStateAdapter.mark_path_breach()`, unregisters the proxy through `SpikeCombatAdapter`, queues it for removal, and lets `SpikeWaveStateAdapter` observe the normal combat `enemy_removed` signal without kill or reward credit.
- Extended `smoke_enemy_proxy.gd` and `smoke_game_state_adapter.gd` to cover one-shot endpoint breach signals, basic-loss wiring from `Main`, combat untracking, wave active removal, and the absence of kill/reward accounting on breaches.
- Added the first Godot path-breach/loss boundary to `SpikeGameStateAdapter`: configured base lives, explicit `mark_path_breach()`, breach counting, terminal `basic_loss`, `is_loss()`, and terminal `is_complete()` behavior.
- Extended `smoke_game_state_adapter.gd` to cover breach events only being accepted while running, non-lethal breaches keeping the wave running, lethal breaches setting `basic_loss`, and next-wave/wave-clear hooks staying blocked after loss.
- Added a debug-only Godot next-wave input hook: pressing `N` in `Main._unhandled_input` calls `start_next_wave_manually()` and therefore only advances after `SpikeGameStateAdapter` reaches `wave_clear`.
- Extended `smoke_game_state_adapter.gd` to cover the debug key boundary: no advancement while a wave is running, advancement after wave clear, second-wave id/index wiring, and `WaveStateAdapter` restart through the existing manual hook.
- Added `Main.start_next_wave_manually()` for the Godot spike: it refuses to run while the current wave is still `running`, starts the next `WaveRunner` wave only after `SpikeGameStateAdapter` reaches `wave_clear`, starts `SpikeWaveStateAdapter` for the new wave id, emits zero-delay spawns, and refuses to run after `basic_win`.
- Extended `smoke_game_state_adapter.gd` with a two-wave scenario fixture to cover the explicit next-wave flow, non-automatic `wave_clear` pause, second-wave state/index/id wiring, final `basic_win`, and the absence of next-wave start after completion.
- Added `SpikeGameStateAdapter` as a thin Godot scenario progression boundary: it tracks configured wave ids, current wave index, and `idle` / `running` / `wave_clear` / `basic_win` states through manual first/next-wave hooks and explicit wave-clear marking.
- Connected `Main` to a scene-level `GameStateAdapter` node for first-wave state tracking and `SpikeWaveStateAdapter.wave_cleared` observation, without adding automatic next-wave scheduling, UI, path breach handling, loss state, rewards, damage, or tower cooldown ownership.
- Added `smoke_game_state_adapter.gd` with a red-green cycle for idle start, first/next wave transitions, non-final wave clear, final `basic_win`, forbidden combat/tower scheduling methods, and the `Main` scene boundary.
- Added a minimal tower fire readability cue in `SpikeTowerAttackAdapter`: every successful tower shot spawns a short-lived `TowerFireCue` near the firing tower, then expires it during adapter advancement without affecting combat state.
- Extended `smoke_tower_attack_adapter.gd` to cover explicit targeting of the nearest live valid enemy while ignoring a dead tracked target, plus fire cue spawning, placement, expiration, and projectile-model-independent behavior.
- Added the visual-only B4 projectile movement preview in `SpikeTowerAttackAdapter`: projectile review nodes now start near the firing tower, move toward the targeted enemy as the adapter advances, and expire after their configured lifetime while damage remains immediate through `SpikeCombatAdapter.apply_damage`.
- Extended `smoke_tower_attack_adapter.gd` to verify projectile start/end positioning, movement over time, expiration, and that visual movement does not apply extra damage outside tower fire timing.
- Added `SpikeWaveStateAdapter` as the first thin Godot wave-state boundary: it starts an active wave with an expected spawn count, registers spawned enemies, listens to `SpikeCombatAdapter.enemy_removed`, tracks active/removed counts, and reports clear only after spawning is complete and no active enemies remain.
- Connected `Main` to a scene-level `WaveStateAdapter` node, starts it for the first `WaveRunner` wave, registers spawned enemies, and marks scheduling complete when `WaveRunner.is_spawning_complete()` becomes true.
- Added `smoke_wave_state_adapter.gd` to verify wave start state, spawn counting, active enemy tracking, combat-driven removal observation, expected spawn gating, clear-state behavior, and the `Main` scene boundary.
- Added `SpikeTowerAttackAdapter` as the first thin Godot tower attack boundary: tower proxies register with catalog stats, enemies are read through `SpikeCombatAdapter.get_tracked_enemies()`, damage is delegated through `combat_adapter.apply_damage`, cooldowns respect `fireRate`, and B4 can spawn a short visual-only projectile review node from `projectileModelPath`.
- Connected `Main` to a scene-level `TowerAttackAdapter` node and registered catalog-spawned towers without moving attack state into `PlaceholderTower` or changing `WaveRunner` scheduling.
- Added `smoke_tower_attack_adapter.gd` and extended `smoke_combat_adapter.gd` to cover tower registration, in-range damage delegation, cooldown behavior, lethal reward/kill accounting through the combat adapter, B4 projectile visual spawning, and the read-only tracked enemy view.
- Integrated the user-provided `shoot.zip` as `shadow-conquest/assets/models/projectiles/shadow_tower_b4_shot.glb`, referenced it from `shadow-tower-b4.projectileModelPath`, and added a static visual-only projectile preview under `PlaceholderTower`.
- Integrated the user-provided `land.zip` as `shadow-conquest/assets/models/terrain/ash_road_surface.glb`, referenced it from `path.surfaceModelPath`, and extended `BoardView` to place visual road surface model instances on path tiles without changing movement/pathing.
- Added `smoke_b4_projectile_asset.gd` and `smoke_road_asset.gd` to verify the new GLBs, catalog references, tower projectile preview attachment, and road surface spawning.
- Replaced the Godot Gondor warrior GLB from the user-provided `gondor.zip`, using `base_basic_pbr.glb` as the new `shadow-conquest/assets/models/enemies/gondor_warrior_proxy.glb` while preserving the existing `gondor-warrior` catalog id and spike wave reference.
- Replaced the Godot Dwarven warrior GLB from the user-provided `gnome.zip`, using `base_basic_pbr.glb` as the new `shadow-conquest/assets/models/enemies/dwarven_warrior_proxy.glb` while preserving the existing `dwarven-warrior` catalog id and spike wave reference.
- Updated `smoke_gondor_enemy_asset.gd` and `smoke_dwarf_enemy_asset.gd` so they verify ZIP-export-sized GLBs, Godot imports, mesh content, and `PlaceholderEnemy.configure_visual(data)` attachment without relying on the older procedural multi-mesh dwarf structure.
- Refreshed `asset_pipeline_spike_preview.png` after replacing the Gondor warrior and Dwarven warrior GLBs, keeping the asset pipeline preview aligned with the latest Free Peoples enemy proxies.
- Integrated the user-provided textured tower archives: `b3.zip` is extracted under `shadow-conquest/assets/models/towers/shadow_tower_b3_textured/` as `base.obj` plus diffuse, normal, metallic, roughness, PBR, and shaded texture files; `b4.zip` is imported as `shadow-conquest/assets/models/towers/shadow_tower_b4.glb`.
- Extended `PlaceholderTower.configure_visual(data)` to support `meshPath` with explicit texture maps and runtime `StandardMaterial3D` setup, while preserving the existing GLB model path flow and primitive fallback.
- Updated `spike_scenario.json` so `shadow-tower-b3` uses the textured OBJ asset stack and added `shadow-tower-b4` as another visual-only tower placement.
- Updated `smoke_shadow_tower_assets.gd` to cover GLB towers, textured OBJ towers, texture imports, catalog entries, placements, and `PlaceholderTower` attachment behavior.
- Refreshed `asset_pipeline_spike_preview.png` so the spike preview shows textured B3 and the new B4 tower candidate with the existing tower, obstacles, terrain props, and enemy proxies.
- Replaced the Godot Elven archer GLB from the user-provided `elf.zip`, using `base_basic_pbr.glb` as the new `shadow-conquest/assets/models/enemies/elf_archer_proxy.glb` while preserving the existing `elven-archer` catalog id and spike wave reference.
- Updated `smoke_elf_enemy_asset.gd` so it verifies the refreshed ZIP-export-sized GLB, its Godot import, mesh content, and `PlaceholderEnemy.configure_visual(data)` attachment without assuming the old multi-mesh procedural structure.
- Refreshed `asset_pipeline_spike_preview.png` after replacing the Elven archer GLB, confirming the new single-mesh Elven proxy appears in the spike scene with the existing health bar and base marker.
- Added user-provided tower GLBs `b2.glb` and `b3.glb` to the Godot project as `shadow-conquest/assets/models/towers/shadow_tower_b2.glb` and `shadow-conquest/assets/models/towers/shadow_tower_b3.glb`, imported them through Godot, wired them as `shadow-tower-b2` and `shadow-tower-b3` in `spike_scenario.json`, and placed both in the spike scene.
- Added catalog-driven tower model support to `PlaceholderTower.configure_visual(data)` and connected `Main` to pass tower catalog entries into placed tower proxies while preserving the existing base model fallback and readability accents.
- Added `smoke_shadow_tower_assets.gd` to verify both new tower GLBs load as `PackedScene`s, contain mesh content, attach through `PlaceholderTower.configure_visual(data)`, avoid the primitive fallback, and are referenced by catalog/placement data.
- Refreshed `asset_pipeline_spike_preview.png` to include the two new imported tower candidates alongside the existing tower, obstacles, terrain props, and Free Peoples enemy proxies.
- Added the user-provided Gondor warrior GLB to the Godot project as `shadow-conquest/assets/models/enemies/gondor_warrior_proxy.glb`, imported it through Godot, wired it as `gondor-warrior` in `spike_scenario.json`, and added it to the first spike wave after the Elven archer and Dwarven warrior.
- Added `smoke_gondor_enemy_asset.gd` to verify the Gondor warrior GLB loads as a `PackedScene`, contains mesh content, attaches through `PlaceholderEnemy.configure_visual(data)`, uses a dedicated readability marker, avoids the primitive fallback, and is referenced by catalog/wave data.
- Refreshed `asset_pipeline_spike_preview.png` to include the new imported Gondor warrior alongside the Elven archer, Dwarven warrior, tower, obstacle proxies, and terrain props.
- Added the first thin Godot combat/state adapter: `SpikeCombatAdapter` registers spawned enemies, delegates damage through enemy `apply_damage(amount)`, listens to `died(enemy)`, untracks and queues dead enemies for removal, and tracks minimal kill/reward totals without changing `WaveRunner` scheduling.
- Connected `Main` to a scene-level `CombatAdapter` node so spawned enemies are registered outside the visual proxy ownership boundary, and added smoke coverage for adapter behavior plus main-scene registration.
- Added the first Godot enemy lifecycle adapter in `PlaceholderEnemy`: catalog health, current/max health, `apply_damage(amount)`, `is_alive()`, `get_health_fraction()`, `health_changed`/`died` signals, death hiding, stopped processing for dead proxies, and compact 3D health bars above enemies.
- Added `smoke_enemy_lifecycle.gd` with a red-green cycle for enemy health setup, non-lethal damage, health bar fill scaling, lethal damage, alive state, and temporary death hiding before full lifecycle removal exists.
- Refreshed `asset_pipeline_spike_preview.png` to verify the imported Elven archer and Dwarven warrior still read with health bars in the full spike scene.
- Added the first Blender-to-Godot terrain prop asset candidate: procedural `Mordor_Rock_Cluster_Proxy`, exported/imported as `shadow-conquest/assets/models/terrain/mordor_rock_cluster_proxy.glb`, with ash patch, black basalt shards, ember cracks, and small Morgul-green lichen accents.
- Added the Godot terrain prop boundary: `World/TerrainProps`, `PlaceholderTerrainProp`, `terrainProps` catalog and placements in `spike_scenario.json`, plus model fit controls through `PlaceholderTerrainProp.configure_visual(data)`.
- Added `smoke_terrain_prop_asset.gd` and `capture_asset_pipeline_preview.gd` to verify the terrain prop GLB path, main-scene spawning, and a visual preview with the tower, obstacle, terrain prop, and imported enemy candidates in one spike scene.
- Added the second Blender-to-Godot Free Peoples enemy asset candidate: procedural `Dwarven_Warrior_Proxy`, exported/imported as `shadow-conquest/assets/models/enemies/dwarven_warrior_proxy.glb`, added to the Godot spike enemy catalog, and spawned after the Elven archer in the first spike wave.
- Generalized imported enemy readability markers in `PlaceholderEnemy.configure_visual(data)` so catalog entries can choose marker names and colors; the Dwarven warrior uses a muted stone marker while the Elven archer keeps its existing stronger green marker.
- Added `smoke_dwarf_enemy_asset.gd` and `capture_free_peoples_enemy_preview.gd` to verify the Dwarven warrior GLB path and capture a preview with both imported Free Peoples enemy candidates moving along the spike path.
- Performed a second Blender/Godot visual pass on `Elf_Archer_Proxy`: added thicker bow, bowstring, nocked arrow, brighter hair, longer ear tips, wider cloak panels, silver trim, and an exported base ring, then re-exported/reimported the GLB and refreshed the Godot spike preview.
- Added a Godot preview capture utility for the spike scene and used it to tune the imported Elven archer under the fixed isometric camera. Increased the catalog fit to `targetHeight=1.15` and `targetFootprint=0.86`, and made the enemy base marker scale from the configured footprint.
- Exported the Blender `Elf_Archer_Proxy` to `shadow-conquest/assets/models/enemies/elf_archer_proxy.glb`, imported it through Godot, added catalog-driven model support to `PlaceholderEnemy`, added `elven-archer` to `spike_scenario.json`, and switched the first spike wave to spawn the imported Elven archer proxy.
- Added `smoke_elf_enemy_asset.gd` to verify the imported Elven archer GLB loads as a `PackedScene`, contains multiple meshes, and attaches through `PlaceholderEnemy.configure_visual(data)` without also building the Gondor fallback.
- Created the first Blender-only Free Peoples enemy asset candidate: procedural `Elf_Archer_Proxy` with bow, bowstring, nocked arrow, quiver, spare arrows, pointed ears, blond hair, green cloak, light armor, base marker, review camera, and render preview. Wrote the short design note `docs/superpowers/specs/2026-05-02-elf-archer-blender-asset-design.md`.
- Fixed the downloadable content and monetization direction in a design spec: content packs use manifests, registry, entitlements, installers, and platform delivery providers; paid gameplay DLC stays isolated from base-campaign balance; visual monetization is cosmetic; claimed event packs remain owned forever.
- Reworked the Godot `PlaceholderEnemy` visual into a procedural Free Peoples / Gondor soldier proxy with primitive body, head, helmet, shield, spear, banner, light metal, blue-grey cloth, and white accents while preserving the existing `setup(path_points, speed)` and movement behavior.
- Added `smoke_enemy_proxy.gd` coverage for enemy scene instancing, setup speed/path placement, expected visual child nodes, and movement along the configured path.

## 2026-05-01

- Added a visual-only Godot obstacle proxy for corrupted roots/webs: JSON `obstacles` catalog and placements, `World/Obstacles`, `PlaceholderObstacle`, smoke coverage for obstacle scene and main-scene spawning, and Android/Web/Windows smoke export verification.
- Tuned the first Godot tower GLB in scene: added exported fit controls, isometric yaw, a dark base marker, red ember beacon/light, and smoke coverage for the visual accent nodes.
- Imported `base.glb` into the Godot project as the first tower GLB, wired `PlaceholderTower` to load and auto-fit it with a primitive fallback, added a tower asset smoke-test, marked `builds/` with `.gdignore`, and verified Android/Web/Windows smoke exports with the model included.
- Added Godot secondary export presets for `Windows Desktop` and `Web Desktop`, produced local smoke builds under `shadow-conquest/builds/`, and verified the Windows debug build starts in headless mode.
- Verified the Godot Android export path: confirmed editor Java/Android SDK settings, added a portable `Android Debug` export preset, enabled ETC2/ASTC texture import for Android validation, produced `builds/android/ShadowConquest-debug.apk`, and verified its APK signatures.
- Completed Godot spike iteration 3: added a portable `WaveRunner` scheduler for JSON `waves` data, connected `Main` to spawn placeholder enemies from wave requests, and added a focused headless smoke test for delay/interval/count scheduling.
- Completed Godot spike iteration 2: reshaped `spike_scenario.json` into portable `board`, `path`, `towers`, `enemies`, and `waves` sections, and updated `Main` to compose tower placements and the enemy preview from the catalog data.
- Completed Godot spike iteration 1: split the primitive scene into `BoardView`, placeholder tower scene, placeholder enemy scene, and a thinner `Main` composition root, with a boundary smoke-test.
- Fixed the next Godot migration work into three short iterations: scene/script boundary split, portable catalog data shape, then a minimal wave runner.
- Added the first Godot spike scene at `shadow-conquest/scenes/main.tscn`: `Node3D` root, orthographic fixed-angle `Camera3D`, primitive XZ tile board, JSON-backed path/tower/enemy placeholders, camera pan/zoom controller, and project main-scene assignment.
- Accepted the Godot production view as 2.5D: `Node3D` world, fixed orthographic `Camera3D`, tile/grid gameplay on XZ, and Compatibility renderer.
- Added the Tripo3D asset pipeline direction: use AI-generated GLB models as rough sources, clean/optimize in Blender, then import into Godot with licensing evidence tracked for shipping assets.
- Wrote the `Godot 2.5D And Asset Pipeline` design spec.
- Created the Godot project at `shadow-conquest/` as `ShadowConquest` with the Compatibility renderer; main scene creation remains the next spike step.
- Installed the Godot Android export toolchain under `D:\Godot`, accepted Android SDK licenses after user approval, and saved `JAVA_HOME`, `ANDROID_HOME`, and `ANDROID_SDK_ROOT` as user environment variables.
- Installed Godot Standard 4.6.2 in self-contained mode under `D:\Godot`, installed matching export templates, installed the Codex Godot skill, and configured GoPeak MCP in compact mode.
- Accepted Godot/GDScript as the production direction for the next version: mobile application first, desktop browser and Windows executable secondary, mobile web out of scope.
- Wrote the Godot migration design spec and updated project notes away from the earlier Android/Kotlin-port framing.
- Completed the first Shadow data/UI pivot: tower families now use Shadow structures, enemy types now represent Free Peoples resistance, campaign waves use the new ids, and UI text frames play as conquest by Shadow while preserving current combat mechanics.
- Remapped renderer dispatch ids to the new Shadow tower and Free Peoples enemy ids while leaving detailed tower art redesign for the dark tower visual pass.
- Wrote the implementation plan for the approved `Shadow Data/UI Pivot`, covering TDD catalog tests, data id migration, UI copy, Obsidian note updates, and final verification.
- Wrote the approved `Shadow Data/UI Pivot` design spec for the first faction-pivot slice: rename data/UI framing to Shadow conquest and Free Peoples resistance without changing combat mechanics or renderer art.
- Moved tower upgrade handling, tower selling, and sell refund value from `src/main.js` into `src/simulation.js`, with focused simulation tests for upgrade spending/effects and sell refunds/removal.
- Cleaned `src/main.js` after the upgrade/sell transfer so it now keeps browser-side status, sound, HUD, and selection refresh around portable simulation mutations.
- Audited the Obsidian vault against current `src/` architecture and updated stale notes in `vision`, `decisions`, `roadmap`, `android-porting`, `mechanics`, `visual-style`, `lore-and-factions`, `iteration-prompts`, and historical `docs/superpowers` plans/specs.
- Made Obsidian documentation upkeep mandatory for every project step, including small refactors, pauses, and housekeeping.
- Added a resume checkpoint to `docs/roadmap.md`: finish `upgradeTower`, `sellTower`, and one `src/main.js` cleanup pass before returning to feature work, then resume with the faction pivot.
- Moved tower placement, gold spending for placement, selected build effects, and placement result tests into `src/simulation.js`; `src/main.js` now keeps only placement status, sound, HUD sync, and selected tower wiring.
- Moved loss, wave-cleared, and level-won outcome transitions into `src/simulation.js`, with browser-side status, sound, saving, and level-switch callbacks kept in `src/main.js`.
- Moved tower cooldown/attack orchestration and projectile/beam/mark/shockwave combat effect creation into `src/simulation.js`, with focused simulation tests for attack execution and kill collection.
- Moved enemy damage application, hit effects, slow handling, splash handling, and focused damage tests into `src/simulation.js`.
- Expanded architecture boundaries:
  - `src/ui.js` owns DOM rendering for HUD, level select, tower buttons, selection actions, tester HUD, status, and sound toggle state.
  - `src/storage.js` owns campaign progress and sound preference persistence.
  - `src/audio.js` owns WebAudio context and sound playback.
  - `src/input.js` owns browser input event wiring for controls and canvas pan/hover/click.
  - `src/simulation.js` now owns spawning timers, enemy creation, enemy movement, target selection helpers, tower placement, tower cooldown/attack orchestration, damage application with hit/slow/splash handling, projectile/beam/mark/shockwave effect creation, kill rewards/effects, outcome transitions, projectile aging, and effect aging.
- Added focused tests for UI, storage, audio, input, renderer, camera, and simulation boundaries.
- Added `src/renderer.js` as the first renderer boundary for top-level draw order, backdrop fill, and visible map traversal.
- Split portable camera/projection math into `src/camera.js` with tests for projection, scale, visible bounds, picking, and camera clamping.
- Added tester menu:
  - unlock all upgrades;
  - add gold;
  - add waves;
  - infinite gold.
- Increased map scale through a logical `200x200` tile.
- Switched to desktop-only layout.
- Added camera drag and wheel panning.
- Increased campaign maps from `32x32` up to `50x50`.
- Added visible tile bounds for map rendering.
- Improved terrain from checker-like tiles to seeded surface variation.
- Experimented with Gondor tower visuals and GPT Image concept.
- Identified current Gondor tower as temporary due to rocket-like silhouette.
- Fixed Black Gate background landmark artifact by replacing blocky rectangles with a subtle silhouette.
- Decided to pivot the fantasy from heroic defense to Shadow conquest.
- Added Obsidian-style project memory files under `docs/`.
